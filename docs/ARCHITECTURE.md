# Architecture

Assets → Production Shots → Production Cues → Run of Service → Live State.

Each camera can use a different individual shot inside the same Production Shot. Congregation shots are included for all three cameras.

All state is stored locally in `data/db.json`.
